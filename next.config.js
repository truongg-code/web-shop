/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "dreamparty.pk",
      "app.testimonialhub.com",
      "plus.unsplash.com",
      "images.unsplash.com",
    ],
  },
};

module.exports = nextConfig;
